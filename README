jquery.listerine.js

@author gehsekky

Listerine manages the display of lists. Currently, it has 2 modes: columns and grid. Columns
mode breaks a list into multiple columns. Grid is basically a horizontal version of columns.